---
phase: 07-pre-merge-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Core/Globals.lua
  - Modules/HideWhenOffCooldown.lua
  - Core/Core.lua
  - Core/GUI.lua
  - Locales/enUS.lua
  - Locales/koKR.lua
  - .pkgmeta
  - BetterCooldownManager.toc
autonomous: true
requirements: []

must_haves:
  truths:
    - "No dead code functions (SetHideWhenOffCooldown, DisableHideWhenOffCooldown) exist in codebase"
    - "All _Dev references reverted to production names in addon source files"
    - "BetterCooldownManager.toc exists, BetterCooldownManager_Dev.toc does not"
    - "Branch diff against main contains only intentional HideSpellOffCD feature additions"
  artifacts:
    - path: "Core/Globals.lua"
      provides: "GetHideWhenOffCooldown and IsHideWhenOffCooldownEnabled (SetHideWhenOffCooldown removed)"
      contains: "function BCDM:GetHideWhenOffCooldown"
    - path: "Modules/HideWhenOffCooldown.lua"
      provides: "RefreshHideWhenOffCooldown (DisableHideWhenOffCooldown removed)"
      contains: "function BCDM:RefreshHideWhenOffCooldown"
    - path: "BetterCooldownManager.toc"
      provides: "Production TOC file with correct SavedVariables and asset paths"
      contains: "SavedVariables: BCDMDB"
  key_links:
    - from: "Core/Globals.lua"
      to: "Modules/HideWhenOffCooldown.lua"
      via: "GetHideWhenOffCooldown still callable by HideWhenOffCooldown module"
      pattern: "GetHideWhenOffCooldown"
---

<objective>
Remove dead code and revert dev-mode references to prepare the HideSpellOffCD branch for merge.

Purpose: Close tech debt identified in v1.0 milestone audit -- two unused functions and dev-mode naming artifacts that should not ship to main.
Output: Clean branch with only intentional feature additions relative to main.
</objective>

<execution_context>
@/home/sntanavaras/.claude/get-shit-done/workflows/execute-plan.md
@/home/sntanavaras/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-pre-merge-cleanup/07-CONTEXT.md
@.planning/phases/07-pre-merge-cleanup/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dead code and revert dev-mode references</name>
  <files>Core/Globals.lua, Modules/HideWhenOffCooldown.lua, Core/Core.lua, Core/GUI.lua, Locales/enUS.lua, Locales/koKR.lua, .pkgmeta, BetterCooldownManager.toc</files>
  <action>
**Step 1 — Delete dead code (two functions, zero call sites):**

In `Core/Globals.lua`, delete lines 650-657 (the `SetHideWhenOffCooldown` function). This is the block:
```lua
function BCDM:SetHideWhenOffCooldown(barType, value)
    if not barType then return end
    local barSettings = self.db.profile.CooldownManager[barType]
    if barSettings then
        barSettings.HideWhenOffCooldown = value
        -- Future phases may add refresh/update calls here
    end
end
```
KEEP the section comment at lines 641-642, KEEP `GetHideWhenOffCooldown` (lines 644-648), and KEEP `IsHideWhenOffCooldownEnabled` (lines 659-663). Only the one function is removed. Preserve a single blank line between `GetHideWhenOffCooldown` end and the `-- Helper:` comment for `IsHideWhenOffCooldownEnabled`.

In `Modules/HideWhenOffCooldown.lua`, delete lines 154-160 (the comment and `DisableHideWhenOffCooldown` function):
```lua
-- Disable the hide-when-off-cooldown feature
function BCDM:DisableHideWhenOffCooldown()
    isEnabled = false

    UnregisterEvents()
    RestoreAllIcons()
end
```
KEEP `RefreshHideWhenOffCooldown` (lines 162-167). Preserve a single blank line between the end of `EnableHideWhenOffCooldown` (line 152) and the `-- Refresh visibility` comment (line 162).

**Step 2 — Revert dev-mode references using toggle-dev.sh:**

First verify dev mode is active: confirm `BetterCooldownManager_Dev.toc` exists.

Run from repo root:
```bash
./toggle-dev.sh prod
```

This handles: TOC rename (mv), SavedVariables in TOC, .pkgmeta package-as, BCDMDB_DEV to BCDMDB in Core.lua, asset paths in all .lua files, addon name strings in all .lua files.

**Step 3 — Handle git staging for the TOC rename:**

The script uses `mv` not `git mv`, so git sees a delete + add. Stage properly:
```bash
git add BetterCooldownManager.toc
git rm BetterCooldownManager_Dev.toc 2>/dev/null || git add -u BetterCooldownManager_Dev.toc
```

**Step 4 — Check the Category-enUS edge case:**

After toggle-dev.sh runs, inspect `BetterCooldownManager.toc` for the `Category-enUS` field. If it still says "Development" or has a dev-specific value, compare against the main branch TOC:
```bash
git show main:BetterCooldownManager.toc | grep "Category-enUS"
```
If it differs, update to match main. If the field does not exist on main, leave as-is (it is a new addition).

**What NOT to do:**
- Do NOT delete toggle-dev.sh (user wants to keep it for future dev work)
- Do NOT delete the section comment "HideWhenOffCooldown Setting API" in Globals.lua (it documents kept functions)
- Do NOT merge to main (cleanup only, merge deferred to user)
  </action>
  <verify>
    <automated>cd /home/sntanavaras/random-projects/BetterCooldownManager && echo "=== Dead code check ===" && grep -r "function BCDM:SetHideWhenOffCooldown\|function BCDM:DisableHideWhenOffCooldown" --include="*.lua" . && echo "FAIL: dead code found" && exit 1 || echo "PASS: no dead code" && echo "=== Dev ref check ===" && DEVHITS=$(grep -r "BetterCooldownManager_Dev\|BCDMDB_DEV" --include="*.lua" --include="*.toc" --include=".pkgmeta" . 2>/dev/null | grep -v toggle-dev.sh || true) && if [ -n "$DEVHITS" ]; then echo "FAIL: dev refs found: $DEVHITS"; exit 1; else echo "PASS: no dev refs"; fi && echo "=== TOC check ===" && ls BetterCooldownManager.toc > /dev/null 2>&1 && echo "PASS: prod TOC exists" && ! ls BetterCooldownManager_Dev.toc > /dev/null 2>&1 && echo "PASS: dev TOC absent" && echo "=== Kept functions check ===" && grep -q "function BCDM:GetHideWhenOffCooldown" Core/Globals.lua && echo "PASS: GetHideWhenOffCooldown kept" && grep -q "function BCDM:IsHideWhenOffCooldownEnabled" Core/Globals.lua && echo "PASS: IsHideWhenOffCooldownEnabled kept" && grep -q "function BCDM:RefreshHideWhenOffCooldown" Modules/HideWhenOffCooldown.lua && echo "PASS: RefreshHideWhenOffCooldown kept" && echo "=== ALL CHECKS PASSED ==="</automated>
  </verify>
  <done>
- SetHideWhenOffCooldown function definition removed from Globals.lua
- DisableHideWhenOffCooldown function definition removed from HideWhenOffCooldown.lua
- All kept functions (Get, IsEnabled, Refresh) still present
- Zero "BetterCooldownManager_Dev" or "BCDMDB_DEV" strings in .lua/.toc/.pkgmeta files
- BetterCooldownManager.toc exists with correct prod values
- BetterCooldownManager_Dev.toc does not exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify branch diff contains only intentional changes</name>
  <files></files>
  <action>
Run the branch diff excluding planning and claude directories:
```bash
git diff main..HEAD -- ':(exclude).planning' ':(exclude).claude' ':(exclude).agents'
```

Review the diff output and confirm:
1. Only HideSpellOffCD feature additions are present (settings infrastructure, cooldown detection, alpha hiding, layout collapse, UI config, API review fixes)
2. No stray `_Dev` references anywhere in the diff
3. No dead code (`SetHideWhenOffCooldown`, `DisableHideWhenOffCooldown`) in the diff
4. The diff for `BetterCooldownManager.toc` shows only the expected new lines (HideWhenOffCooldown module inclusion, if any)
5. `toggle-dev.sh` appears in the diff as a new file (expected -- it is a dev tool being kept)

If any unexpected changes are found, fix them before completing this task. Expected files in the diff:
- Core/Globals.lua (new setting functions + cooldown detection API)
- Core/Core.lua (no _Dev changes, only feature additions if any)
- Core/GUI.lua (checkbox UI for HideWhenOffCooldown)
- Modules/HideWhenOffCooldown.lua (new module)
- Locales/enUS.lua, Locales/koKR.lua (new locale strings)
- BetterCooldownManager.toc (module inclusion)
- .pkgmeta (if changed for module)
- toggle-dev.sh (new file, kept for future dev)
  </action>
  <verify>
    <automated>cd /home/sntanavaras/random-projects/BetterCooldownManager && echo "=== Diff summary ===" && git diff --stat main..HEAD -- ':(exclude).planning' ':(exclude).claude' ':(exclude).agents' && echo "=== Checking for stray _Dev in diff ===" && DEVINDIFF=$(git diff main..HEAD -- ':(exclude).planning' ':(exclude).claude' ':(exclude).agents' ':(exclude)toggle-dev.sh' | grep "^+" | grep -i "BetterCooldownManager_Dev\|BCDMDB_DEV" || true) && if [ -n "$DEVINDIFF" ]; then echo "FAIL: _Dev references in diff additions: $DEVINDIFF"; exit 1; else echo "PASS: no _Dev in diff additions"; fi && echo "=== Checking for dead code in diff ===" && DEADINDIFF=$(git diff main..HEAD -- ':(exclude).planning' ':(exclude).claude' | grep "^+" | grep "function BCDM:SetHideWhenOffCooldown\|function BCDM:DisableHideWhenOffCooldown" || true) && if [ -n "$DEADINDIFF" ]; then echo "FAIL: dead code in diff: $DEADINDIFF"; exit 1; else echo "PASS: no dead code in diff"; fi && echo "=== ALL DIFF CHECKS PASSED ==="</automated>
  </verify>
  <done>
- Branch diff against main shows only intentional HideSpellOffCD feature additions
- No _Dev references in diff additions (excluding toggle-dev.sh)
- No dead code function definitions in diff additions
- Diff stat reviewed and all changed files are expected
  </done>
</task>

</tasks>

<verification>
All verification is grep and diff based (no test framework in this WoW addon project):

1. Dead code removal confirmed by grep returning zero matches for function definitions
2. Dev reference cleanup confirmed by grep returning zero matches across .lua/.toc/.pkgmeta
3. TOC file state confirmed by filesystem check
4. Kept functions confirmed present by grep
5. Branch cleanliness confirmed by diff review excluding planning directories
</verification>

<success_criteria>
- Zero `SetHideWhenOffCooldown` or `DisableHideWhenOffCooldown` function definitions in codebase
- Zero `BetterCooldownManager_Dev` or `BCDMDB_DEV` strings in addon source files
- `BetterCooldownManager.toc` exists with production values; `BetterCooldownManager_Dev.toc` absent
- Branch diff against main contains only intentional feature additions
- `toggle-dev.sh` retained for future use
</success_criteria>

<output>
After completion, create `.planning/phases/07-pre-merge-cleanup/07-01-SUMMARY.md`
</output>
